* Get this module tracking the very bleeding edge of kernel development. It's
  already at the latest stable (as of this writing) kernel, but is not yet 
  known to work with the very latest latest kernels.

* Get this module building against other distributions' default kernels, like
  whatever the various versions of Ubuntu ship with.

* Make installation process smoother.
